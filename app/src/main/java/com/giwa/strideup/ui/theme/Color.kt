package com.giwa.strideup.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * StrideUp "Cream & Sunset" 팔레트.
 *
 * 다크 럭셔리 대신 따뜻한 크림 캔버스 위에 선셋 코럴을 얹는다.
 * 캐주얼함은 색과 곡률에서, 고급감은 여백·타이포·부드러운 그림자에서 나온다.
 * 코럴/허니는 항상 2~3단 그라데이션으로만 쓰고 단색 면으로 칠하지 않는다.
 */

// ── Canvas: 크림 린넨 ─────────────────────────────────────────
/** 앱 배경 (따뜻한 크림) */
val Cream = Color(0xFFF9F5EE)

/** 배경 하단 단계 */
val CreamDeep = Color(0xFFF1EADC)

/** 카드 표면 */
val CardWhite = Color(0xFFFFFFFF)

/** 카드 상단(아주 옅은 웜톤으로 종이 질감을 만든다) */
val CardWarm = Color(0xFFFFFDF9)

/** 인셋 웰 · 게이지 트랙 */
val Sand = Color(0xFFEFE7D7)

/** 카드 헤어라인 */
val Border = Color(0xFFEDE4D4)

// ── Accent: 선셋 ─────────────────────────────────────────────
/** 주 강조색 — 선셋 코럴 */
val Coral = Color(0xFFFF6B4A)

/** 코럴 음영 */
val CoralDeep = Color(0xFFF04E2E)

/** 그라데이션 파트너 — 살구 */
val Peach = Color(0xFFFFA94D)

/** SUP 포인트 · 리워드 */
val Honey = Color(0xFFEFA92F)

/** 허니 음영 */
val HoneyDeep = Color(0xFFD98E14)

// ── Supporting ───────────────────────────────────────────────
/** 적립 · 성공 */
val Sage = Color(0xFF6FA57C)

/** 종료 · 차감 */
val Rose = Color(0xFFE05B5B)

// ── Text ─────────────────────────────────────────────────────
/** 본문 잉크 (웜 블랙) */
val Ink = Color(0xFF252016)

/** 보조 텍스트 */
val Taupe = Color(0xFF8C8170)

/** 힌트 · 비활성 */
val TaupeLight = Color(0xFFBAAF9C)
