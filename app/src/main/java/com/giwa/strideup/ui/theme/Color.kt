package com.giwa.strideup.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * StrideUp "Obsidian & Champagne" 팔레트.
 *
 * 네온 게이밍 톤 대신 흑요석(obsidian) 베이스 + 샴페인 골드 메탈릭으로 구성한다.
 * 골드는 항상 3단(deep / base / light)으로 다뤄 그라데이션에서 금속 광택이 살아나게 한다.
 */

// ── Base: 흑요석 ──────────────────────────────────────────────
/** 최심부 — 네비게이션 바·비네트 */
val ObsidianDeep = Color(0xFF050609)

/** 앱 배경 */
val Obsidian = Color(0xFF08090D)

/** 배경 상단(미세한 명암차로 깊이를 만든다) */
val ObsidianTop = Color(0xFF0D0F15)

/** 카드 베이스 */
val Charcoal = Color(0xFF0E1015)

/** 카드 상단 단계 / 비활성 컨트롤 */
val CharcoalHigh = Color(0xFF171A22)

/** 헤어라인 외곽선 */
val Hairline = Color(0xFF232732)

// ── Accent: 샴페인 골드 ───────────────────────────────────────
/** 금속 음영 */
val ChampagneDeep = Color(0xFF9A7642)

/** 주 강조색 */
val Champagne = Color(0xFFE0C08A)

/** 금속 하이라이트 */
val ChampagneLight = Color(0xFFF8ECD2)

// ── Supporting ───────────────────────────────────────────────
/** 보조 강조 — 거리·목표 */
val Platinum = Color(0xFFD7DCE4)

/** 적립·성공 */
val Jade = Color(0xFF7CBBA0)

/** 칼로리 */
val Copper = Color(0xFFC9865C)

/** 종료·차감 */
val Crimson = Color(0xFFCF5A54)

// ── Text ─────────────────────────────────────────────────────
val Ivory = Color(0xFFF3F1EC)
val Ash = Color(0xFF8B909B)
val AshDim = Color(0xFF5A5F6A)
