name = "stepup-attester"
main = "src/index.js"
compatibility_date = "2026-01-01"
compatibility_flags = ["nodejs_compat"]

# GIWA Sepolia (91342) 에 배포된 RewardDistributor
# https://sepolia-explorer.giwa.io/address/0x9f9E87bD825144A8315d30979E3004FbaCFE36E1
[vars]
DISTRIBUTOR_ADDRESS = "0x9f9E87bD825144A8315d30979E3004FbaCFE36E1"

# 서명 키는 vars 가 아니라 Secret 으로 넣는다:
#   npx wrangler secret put ATTESTER_PRIVATE_KEY
# Secret 은 대시보드에서도 다시 볼 수 없고, 저장소에도 남지 않는다.
